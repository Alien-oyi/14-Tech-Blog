<section class="card">
  <a href="/post/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <p>{{content}}</p>
      <div>Post By: {{user_id}} on {{format_date created_at}}</div>
    </div>
  </a>
</section>